# NEXUS v7 — Dependencies
# Lean, clean, no bloat

# Core
pyyaml>=6.0
requests>=2.31
aiohttp>=3.9

# Telegram
telethon>=1.34

# Discord (contributor onboarding bot)
discord.py>=2.3

# Web search (optional — fallback uses requests)
duckduckgo-search>=4.0

# Future: vector search for better memory recall
# sentence-transformers>=2.2
# faiss-cpu>=1.7